package site.mohememd.CarsBackend.car.filterOptionsProvider2.options;

public class CarHandleStatus extends CarOption {
    public CarHandleStatus(int optionId, String optionName) {
        super(optionId, optionName);
    }
}
